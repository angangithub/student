<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zto.onecard.service.base.egroup.mapper.ScoreManagementMapper">

	<!-- resultMap 基本 -->
    <resultMap type="com.zto.onecard.service.base.egroup.entity.ScoreManagementEntity" id="scoreManagementMap">
                    <result property="id" column="id"/>
                    <result property="studentId" column="student_id"/>
                    <result property="courseId" column="course_id"/>
                    <result property="score" column="score"/>
                    <result property="remark" column="remark"/>
            </resultMap>

    <!-- 基础表字段 -->
    <sql id="ScoreManagement_basic_sql">
                    id,                    student_id,                    course_id,                    score,                    remark            </sql>

    <!-- 表名 -->
    <sql id="ScoreManagement_tableName">
        score_management
    </sql>

    <!-- 单个表添加 -->
    <insert id="create" parameterType="com.zto.onecard.service.base.egroup.entity.ScoreManagementEntity" useGeneratedKeys="true" keyProperty="id">
        insert into score_management
        <trim prefix="(" suffix=")" suffixOverrides="," >
                            <if test="id != null" >
                    id,                </if>
                            <if test="studentId != null" >
                    student_id,                </if>
                            <if test="courseId != null" >
                    course_id,                </if>
                            <if test="score != null" >
                    score,                </if>
                            <if test="remark != null" >
                    remark                </if>
                    </trim>
        <trim prefix="values (" suffix=")" suffixOverrides="," >
                            <if test="id != null" >
                    #{id},                </if>
                            <if test="studentId != null" >
                    #{studentId},                </if>
                            <if test="courseId != null" >
                    #{courseId},                </if>
                            <if test="score != null" >
                    #{score},                </if>
                            <if test="remark != null" >
                    #{remark}                </if>
                    </trim>
    </insert>

    <!-- 分页搜索查询 -->
    <select id="queryForPage" resultMap="scoreManagementMap">
        select
        <include refid="ScoreManagement_basic_sql" />
        from
        <include refid="ScoreManagement_tableName" />
        <where>
            <if test="params.id != null">
                and id = #{params.id}
            </if>
            
        </where>
    </select>

    <select id="queryById" resultMap="scoreManagementMap">
        select
        <include refid="ScoreManagement_basic_sql" />
        from
        <include refid="ScoreManagement_tableName" />
        where id = #{id}
    </select>

    <select id="queryCount" resultType="int">
        select count(*) from <include refid="ScoreManagement_tableName" />
    </select>

    <!-- 修改操作 -->
    <update id="modifyById">
        update  <include refid="ScoreManagement_tableName" />
        <set>
                            <if test="id != null" >
                    id = #{id},                </if>
                            <if test="studentId != null" >
                    student_id = #{studentId},                </if>
                            <if test="courseId != null" >
                    course_id = #{courseId},                </if>
                            <if test="score != null" >
                    score = #{score},                </if>
                            <if test="remark != null" >
                    remark = #{remark}                </if>
                    </set>
        where id = #{id}
    </update>

    <delete id="delById">
        delete from <include refid="ScoreManagement_tableName" /> where id = #{id}
    </delete>

    <delete id="delByIds">
        delete from <include refid="ScoreManagement_tableName" /> where id in
        <foreach collection="ids" open="(" close=")" separator="," item="item">
            #{item}
        </foreach>
    </delete>



</mapper>